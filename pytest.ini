# Keep root pytest for infrastructure artifact tests only.
# Application suites must run in isolated processes so `app`/`config`
# modules do not collide (see scripts/validate_pov.py).
[pytest]
testpaths = tests
