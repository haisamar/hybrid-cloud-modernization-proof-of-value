# Template only. Do not commit real credentials.
# A future shared database or ERP credential would be injected here.
apiVersion: v1
kind: Secret
metadata:
  name: order-intake-secrets
  namespace: order-intake-pov
  labels:
    app.kubernetes.io/name: order-intake
type: Opaque
stringData:
  ERP_HANDOFF_TOKEN: "CHANGE_ME"
  FUTURE_DB_PASSWORD: "CHANGE_ME"
